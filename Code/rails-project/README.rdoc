== README

Installing the application:

* mkdir project; cd project;

* git clone git@github.com:ablagoev/nbu-course-citb608.git

* cd Code/rails-project

* bundle install

* bundle exec rake db:setup

* bundle exec rake db:migrate

* bundle exec rails s
